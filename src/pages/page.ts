enum page {
    HOME = "/",
    BROWSE = "/browse",
    LOGIN = "login",
    SIGNUP = '/signup',
    GENRE = '/browse/genre/:genre',
    SEARCH = '/search',
    TITLE = "/title/:id",
    MODAL = "/browse/m/:type/:id",
}

export default page