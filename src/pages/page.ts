enum page {
    HOME = "/",
    BROWSE = "/browse",
    LOGIN = "login",
    SIGNUP = '/signup',
    GENRE = '/genre/:genre',
    SEARCH = '/search',
    TITLE = "/title/:id",
    MODAL = "/m/:type/:id",
    LINK_MODAL = "/browse/m/:type/:id",
}

export default page