enum page {
    HOME = "/",
    BROWSE = "/browse",
    LOGIN = "login",
    SIGNUP = '/signup',
    GENRE = '/browse/genre/:genre',
    SEARCH = '/search',
    TITLE = "/title/:id",
    PERSON = "/person/:id"
}

export default page