import React from 'react';

interface Props {
    data: Object
}
const BrowseContainer = ({data}: Props) => {
    
    return (
        <div>
            
        </div>
    );
};

export default BrowseContainer;